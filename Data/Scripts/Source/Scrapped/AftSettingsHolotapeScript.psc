Scriptname AFT:AftSettingsHolotapeScript extends ReferenceAlias

; Event OnContainerChanged(ObjectReference akNewContainer, ObjectReference akOldContainer)
	;if (Game.GetPlayer() == akOldContainer)
		; TODO : Do we want to restore it if they attempt to drop it?
	; endif
; EndEvent
